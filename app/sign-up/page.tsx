'use client'

import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import { authClient } from '@/lib/auth-client'

export default function SignUpPage() { const router = useRouter(); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setError(''); if (!email.toLowerCase().endsWith('@vietinbank.vn')) { setError('Chỉ dành cho email nội bộ Vietinbank (@vietinbank.vn).'); setLoading(false); return } const result = await authClient.signUp.email({ name, email, password }); if (result.error) setError('Không thể tạo tài khoản. Kiểm tra thông tin và thử lại.'); else { router.push('/onboarding'); router.refresh() }; setLoading(false) } return <main className="flex min-h-screen items-center justify-center bg-transparent px-5"><section className="w-full max-w-md rounded-3xl bg-white p-8 shadow-xl shadow-[#552c2012] ring-1 ring-[#eadfd8]"><div className="mb-8 text-center"><div className="mx-auto mb-4 flex size-12 items-center justify-center rounded-2xl bg-[#9f1d35] text-xl text-white">♥</div><h1 className="font-serif text-3xl text-[#392b27]">Tham gia Datin Vietin</h1><p className="mt-2 text-sm text-[#9f8a7d]">Đầu tư đúng chỗ, yêu thương đúng người.</p></div><form onSubmit={submit} className="flex flex-col gap-4"><input required placeholder="Họ và tên" value={name} onChange={(e) => setName(e.target.value)} className="rounded-xl border border-[#eadfd8] px-4 py-3 outline-none focus:border-[#9f1d35]" /><input required type="email" placeholder="email@vietinbank.vn" value={email} onChange={(e) => setEmail(e.target.value)} className="rounded-xl border border-[#eadfd8] px-4 py-3 outline-none focus:border-[#9f1d35]" /><input required minLength={8} type="password" placeholder="Mật khẩu tối thiểu 8 ký tự" value={password} onChange={(e) => setPassword(e.target.value)} className="rounded-xl border border-[#eadfd8] px-4 py-3 outline-none focus:border-[#9f1d35]" />{error && <p className="text-sm text-[#9f1d35]">{error}</p>}<button disabled={loading} className="rounded-xl bg-[#9f1d35] py-3 font-semibold text-white disabled:opacity-60">{loading ? 'Đang tạo tài khoản...' : 'Đăng ký'}</button><p className="text-center text-sm text-[#806e64]">Đã có tài khoản? <Link className="font-semibold text-[#9f1d35]" href="/sign-in">Đăng nhập</Link></p></form></section></main> }
